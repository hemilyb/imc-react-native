import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  boxTitle: {
    alignItems: "center",
    justifyContent: "center",
    padding: 10
  },
  textTitle: {
    color: "#5E6EC4",
    fontSize: 24,
    fontWeight: "bold",
  }
})